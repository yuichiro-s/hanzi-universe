from distutils.core import setup

from setuptools import find_packages

setup(
    name='hanzi_universe',
    version='0.0.1',
    description='',
    packages=find_packages(),
    requires=[
        'flask',
    ])
